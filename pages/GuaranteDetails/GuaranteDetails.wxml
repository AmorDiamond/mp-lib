<!--pages/GuaranteDetails/GuaranteDetails.wxml-->
<wxs src="../../utils/commtime.wxs" module="formtime"/>
<van-tabs active="{{ active }}" animated bind:click="onChangeScorll" color="#0969AD">
 <van-tab title="{{item.brandName}}"  name="{{item.brandCode}}" wx:for="{{tabList}}" wx:key="index">

<view class="waperwile">
   <view class="wilthebox">
<!-- ===== -->
       <view class="weapergsts">
         <view class="gscolor">开始时间</view>
         <view class="luozucolor">
        <picker mode="date" value="{{startTime}}" class="weaperpciker" bindchange="bindDateChange">
          <view class="picker">
            {{startTime}}
          </view>
        </picker>
          <image src="../../img/shijsxia.png" class="shangximg"></image>
           </view>
       </view>
<!-- ==== -->
       <view class="weapergs">
         <view class="gscolor">结束时间</view>
         <view class="luozucolor">
          <picker mode="date" value="{{endTime}}" 
          class="weaperpciker" 
          bindchange="bindDateChangeend">
          <view class="picker">
            {{endTime}}
          </view>
         </picker>
          <image src="../../img/shijsxia.png" class="shangximg"></image>
         </view>
       </view>

   </view>
</view>


<!-- =============凭证 -->
<scroll-view scroll-y style="height:calc(100vh - 203px)" bindscrolltolower="scrolltolower">
<view class="xianwhileboxwea" wx:for="{{descrList}}" wx:key="index">
  <view class="xianwhilebox">
  <view class="jizTime"><view>凭证编号  {{item.credentialCode||''}}</view><view>{{item.changeTime||""}}</view></view>
    

   <view class="boxjief">
    <text>借方：</text><text class="moneycol">{{item.lendMoney?'￥'+item.lendMoney:''}}</text>
   </view>

  <view class="boxjief">
    <text>贷方：</text><text class="moneycol">{{item.loanMoney?'￥'+item.loanMoney:''}}</text>
  </view>


  <view class="boxjief">
    <text>余额：</text><text class="moneycol">{{item.balance?'￥'+item.balance:''}}</text>
  </view>


   <view class="boxjief">
    <text>总账标识：</text><text class="textzi">{{item.totalAccountFlag||''}}</text>
   </view>

   <view class="boxjief">
    <text>原因代码：</text><text class="textzi">{{item.causeCode||""}}</text>
   </view>

    <view class="boxjief">
    <text>备注：</text><text class="textzi">{{item.description||''}}</text>
  </view>
 </view>
</view>


<view class="height"></view>
   <!-- ====没有数据背景 -->
  <wux-nothing-page wx:if="{{!descrList||descrList.length==0}}" resetStyle="height:calc(100vh - 215px)"></wux-nothing-page>
</scroll-view>





  </van-tab>
</van-tabs>

<view class="weaper">
<view class="flloter">
  <view class="hejtext">合计</view>
  <view class="zhanktext" bindtap="handeltomore">
     <view wx:if="{{!showmore}}">展开</view>
     <view wx:if="{{showmore}}" >收起</view>

    <image src="../../img/jruon.png"  class="jruon" wx:if="{{!showmore}}" />
    <image src="../../img/jrutw.png"  class="jruon" wx:if="{{showmore}}" />

  </view>
</view>


  <view wx:if="{{showmore}}" class="jiefang">

   <view class="flextbox">
     <view>借方</view>
     <view>{{total.sumLendMoney||""}}</view>
   </view>

   <view class="flextbox">
     <view>贷方</view>
     <view>{{total.sumLoanMoney||""}}</view>
   </view>

   <view class="flextbox">
     <view>余额</view>
     <view>{{total.sumBalance||""}}</view>
   </view>


 </view>

</view>
